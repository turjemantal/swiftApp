# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'chatApplications' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for chatApplications
    pod 'Firebase/Auth'
    pod 'Firebase/Firestore'

  target 'chatApplicationsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'chatApplicationsUITests' do
    # Pods for testing
  end

end
